image: node:latest

stages:
  - install
  - test

cache:
  paths:
    - node_modules/

install:
  stage: install
  script:
    - npm install

test:
  stage: test
  script:
    - npm install
    - npm run test

lint:
  stage: test
  script:
    - npm install
    - npm run lint
